server {
    listen 80;
    server_name _;

    root /var/www/angular-app;
    index index.html;

    # phpMyAdmin
    location /phpmyadmin {
        root /usr/share/;
        index index.php;
        location ~ ^/phpmyadmin/(.+\.php)$ {
            try_files $uri =404;
            fastcgi_pass unix:/run/php/php8.3-fpm.sock;
            fastcgi_index index.php;
            fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
            include fastcgi_params;
        }
    }

    location /phpMyAdmin {
        rewrite ^/* /phpmyadmin last;
    }

# ✅ Permitir acceso solo a procesar_orden.php
location = /backend/procesar_orden.php {
    include snippets/fastcgi-php.conf;
    fastcgi_pass unix:/run/php/php8.3-fpm.sock;
    fastcgi_param SCRIPT_FILENAME /var/www/backend/procesar_orden.php;
}


    # ✅ Permitir stripe_checkout.php si está en /angular-app/api/
    location /api/stripe_checkout.php {
        include snippets/fastcgi-php.conf;
        fastcgi_pass unix:/run/php/php8.3-fpm.sock;
        fastcgi_param SCRIPT_FILENAME /var/www/angular-app/api/stripe_checkout.php;
    }
# ✅ Permitir acceso solo a get_usuario.php
location = /backend/get_usuario.php {
    include snippets/fastcgi-php.conf;
    fastcgi_pass unix:/run/php/php8.3-fpm.sock;
    fastcgi_param SCRIPT_FILENAME /var/www/backend/get_usuario.php;
}


    # 🚫 Bloquear el acceso al resto del backend por seguridad
    location /backend {
        deny all;
        return 403;
    }

    # 🚫 Bloquear acceso directo a archivos ocultos (.env, .htaccess, etc.)
    location ~ /\. {
        deny all;
    }

    # Angular frontend routing
    location / {
        try_files $uri /index.html;
    }

    # PHP general (solo para archivos del frontend, si es necesario)

    # PHP general (solo para archivos del frontend, si es necesario)
    location ~ \.php$ {
        root /var/www/angular-app;
        include snippets/fastcgi-php.conf;
        fastcgi_pass unix:/run/php/php8.3-fpm.sock;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        include fastcgi_params;
    }
}
